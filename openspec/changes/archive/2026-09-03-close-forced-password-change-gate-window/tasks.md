## 1. Backend prerequisite (separate repo — coordinate, do not implement here)

- [x] 1.0 **Revised 2026-09-03, then corrected again same day**: an earlier pass of this task concluded `POST /auth/change-password` didn't exist, based on the checked-in `openapi/openapi.json` snapshot and a stale comment at `src/lib/api/endpoints.ts:3-6`. A local backend became reachable later in the session; a live call confirmed **the endpoint does exist** — it whitelist-validates its body and returned 401 `"Current password is incorrect"` for a wrong current password. That call also caught a real bug: `authService.changePassword()` was forwarding `confirmPassword`, which the backend rejects as an unknown property — fixed to send only `{ currentPassword, newPassword }`. `mustChangePassword` is still absent from the login response in that same live check, so tasks 1.1/1.2/1.4 below remain genuinely open.
- [x] 1.1 Confirm with the backend repo that `mustChangePassword` is added to the access-token claims in the JWT signing path (both the login and refresh paths, so the claim survives a refresh) — **confirmed shipped, 2026-09-03**: a fresh live login now returns `mustChangePassword: false` on both the access and refresh token payloads, correctly typed as a boolean. **Also observed** (out of scope for this change, flagging separately): `role` (singular string) is now `roles` (array), plus new `managedBranches` and `typ` fields. `branches` itself is unchanged (still `number[]`). `userFromAccessToken()` in `auth.service.ts` still reads `claims.role` per the old shape, so `AuthUser.role` is likely always `undefined` now. Not fixed here - unrelated to the password gate and affects the existing `auth` capability broadly, not just this change.
- [x] 1.2 Confirm the force-password-change guard's `403` body shape and the exact discriminator key/value for `PASSWORD_CHANGE_REQUIRED` — resolves the second Open Question in `design.md`. **Resolved 2026-09-03**: the `staffhub-backend` repo turned out to be checked out locally on this machine (`C:\_hub\berd\berd.em-backend`, running via a local NestJS process) — read directly instead of guessing further. `ForcePasswordChangeGuard` (`src/modules/auth/guards/force-password-change.guard.ts`) throws `new ForbiddenException({ message: 'Password change required before continuing', details: { code: 'PASSWORD_CHANGE_REQUIRED' } })`; `GlobalExceptionFilter` forwards `details` verbatim into the JSON response. **Confirmed live** by provisioning a real flagged employee (`POST /employees`, which - unlike `POST /users` - creates a linked User with a genuine one-time password and `mustChangePassword: true`; the temp password comes back in the response as `temporaryPassword`), logging in as it, and hitting a protected endpoint while flagged: response was exactly `{"statusCode":403,"message":"Password change required before continuing","source":"Application","details":{"code":"PASSWORD_CHANGE_REQUIRED"},"timestamp":"..."}`. `src/lib/api/errors.ts`'s `isPasswordChangeRequired()` was narrowed from defensive multi-key guessing to `data?.details?.code === "PASSWORD_CHANGE_REQUIRED"` to match. Test employee + its linked user record were deleted afterward (`DELETE /employees/19` then `DELETE /users/27`, both `204`) - no lasting data left behind. **Resolves the earlier "no live path to provision a flagged account" concern too** - that was calling the wrong endpoint (`/users` instead of `/employees`); the intended provisioning flow works exactly as the proposal assumed.
- [x] 1.3 Confirm `POST /auth/change-password` exists and works for a normal (unflagged) call, and that the guard exempts it for a *flagged* user — both confirmed live 2026-09-03. Source confirms the exemption explicitly: `auth.controller.ts`'s `changePassword` handler carries `@AllowWhilePasswordChangeRequired()`. Live: the same flagged test employee above successfully called `POST /auth/change-password` (`200`, no 403) while still carrying `mustChangePassword: true`.
- [x] 1.4 Confirm whether `POST /auth/change-password` returns a fresh token pair or the frontend must call `POST /auth/refresh` afterward — **confirmed live 2026-09-03** (per user approval): a successful call returns only `{"message":"Password changed successfully."}`, no tokens. The refresh-after approach in task 5.2 is required, confirmed by direct test

## 2. Types and token decoding

- [x] 2.1 Add optional `mustChangePassword?: boolean` to `AccessTokenClaims` in `src/features/auth/types/auth.type.ts`, with a comment noting the claim is absent on tokens minted before the backend change
- [x] 2.2 Add optional `mustChangePassword?: boolean` to `AuthUser` in the same file
- [x] 2.3 Map the claim onto the derived user in `userFromAccessToken()` in `src/features/auth/services/auth.service.ts`, reading it as `claims.mustChangePassword === true` so a missing claim means "not flagged"

## 3. Server-side gate in middleware

- [x] 3.1 In `src/middleware.ts`, decode the access token once and reuse the claims for both the existing expiry check and the new flag check, rather than calling `decodeJwt` twice
- [x] 3.2 Add the flagged-user branch **after** the existing missing/expired-token redirect and after the authenticated-user-on-public-path redirect, so an expired token still goes to `/login`
- [x] 3.3 Redirect a flagged request targeting any path other than `/change-password` to `/change-password`, carrying the original pathname and query string via `buildReturnUrl` as a `returnUrl` param
- [x] 3.4 Let a flagged request targeting `/change-password` through untouched; verify `/change-password` is **not** added to `PUBLIC_PATHS`, so an anonymous request for it still redirects to `/login`
- [x] 3.5 Add a short comment explaining why this check fails open on a missing claim while `isTokenExpired` fails closed on an undecodable token

## 4. Change-password screen

- [x] 4.1 Create `src/features/auth/components/ChangePasswordForm.tsx` following the `LoginForm.tsx` convention (`useForm` + `zodResolver(changePasswordSchema)` + shadcn `Form`/`FormField`/`Input`/`Button` inside a `Card`), with `currentPassword`, `newPassword`, and `confirmPassword` fields and no default values
- [x] 4.2 Write all user-facing copy in Vietnamese per project convention (e.g. "Đổi mật khẩu", "Mật khẩu hiện tại", "Mật khẩu mới", "Xác nhận mật khẩu mới"), with a short line explaining why the user is here
- [x] 4.3 Ensure the form offers no navigation into the gated app — no links to the dashboard, no back action
- [x] 4.4 Create `src/features/auth/hooks/useChangePassword.ts` wrapping `authService.changePassword` following the `useLogin.ts` pattern, surfacing `isPending` for the submit button and the backend's error message on failure
- [x] 4.5 Create `src/app/(auth)/change-password/page.tsx` rendering `ChangePasswordForm`, matching the centred layout used by `src/app/(auth)/login/page.tsx`

## 5. Release path after a successful change

- [x] 5.1 Expose a way for the change-password flow to update `AuthContext` state after success (the context already exposes `refreshAccessToken`; add what the form needs rather than mutating cookies behind the context's back)
- [x] 5.2 Per the answer to task 1.4: either persist the returned token pair via `tokenManager.setTokens` and update context state, or call `refreshAccessToken()` — so the stored token no longer carries `mustChangePassword: true` — implemented via a direct `authService.refreshToken()` call (not the context's `refreshAccessToken()` wrapper, which force-logs-out on failure and would violate 5.4). **Confirmed correct choice**: a live call to `POST /auth/change-password` (2026-09-03, against a reachable local backend, immediately reverted afterward) returned only `{"message":"Password changed successfully."}` — no token pair — so the refresh-after approach is required, not just a defensive fallback
- [x] 5.3 Only after the session is refreshed, navigate to the `returnUrl` query param resolved through `resolveReturnUrl(..., '/')`, so the same-origin check that protects login applies here too
- [x] 5.4 On a failed refresh, keep the user on `/change-password` with an error rather than navigating into a middleware bounce loop

## 6. Proactive gate at login

- [x] 6.1 In `login()` in `src/features/auth/context/AuthContext.tsx`, resolve the destination with `resolveReturnUrl` as it does today, then branch on the returned user's `mustChangePassword`
- [x] 6.2 When flagged, navigate to `/change-password` carrying the resolved destination as an encoded `returnUrl` param; when not flagged, navigate to the resolved destination exactly as before
- [x] 6.3 Leave the success toast and the error path unchanged

## 7. Reactive 403 fallback

- [x] 7.1 Add a `isPasswordChangeRequired(error: AxiosError): boolean` helper to `src/lib/api/errors.ts` alongside `getFieldErrors`, matching the body shape confirmed in task 1.2 and pinning the discriminator in one place — **narrowed 2026-09-03** to the confirmed real shape, `data?.details?.code === "PASSWORD_CHANGE_REQUIRED"`, replacing the earlier defensive multi-key guess
- [x] 7.2 In the response interceptor in `src/lib/api/axios.ts`, add a `403` branch that dispatches an `auth:password-change-required` window event and rejects the promise, mirroring the existing `auth:session-expired` dispatch style
- [x] 7.3 Ensure this branch does **not** call `tokenManager.clearTokens()` — the session is valid, only gated — and that an ordinary `403` without the discriminator falls through to the existing reject with no event
- [x] 7.4 Ensure the existing `401` refresh/queue path is untouched by the new branch
- [x] 7.5 In `AuthContext.tsx`, add an `auth:password-change-required` listener alongside the existing `auth:session-expired` effect that navigates to `/change-password` with the current full path (pathname, search, hash) as a `returnUrl` via `buildReturnUrl`, without clearing user or token state

## 8. Verification

- [x] 8.1 Run `pnpm lint` and `pnpm build` clean — both fail, but identically on `develop` before this change (confirmed via `git stash`): repo-wide pre-existing lint debt (`no-undef 'React'`, unused-vars, `no-explicit-any`) unrelated to this change. TypeScript compilation itself succeeds ("Compiled successfully"); the new code follows the exact same existing (already-broken) conventions used elsewhere in `AuthContext.tsx`/`auth.type.ts` (e.g. `catch (error: any)`, interface param names) and introduces no new error category. Not fixed here — out of scope for this change.
- [x] 8.2 With no `mustChangePassword` claim in circulation, confirm login, navigation, refresh, and logout behave exactly as before — verified via `pnpm dev` + crafted unflagged JWT cookie: `/employees` returns 200 (no gate), `/login` with a valid session redirects to `/` as before
- [x] 8.3 With a flagged token: log in and confirm the redirect to `/change-password`, including the preserved `returnUrl` when arriving from a bounced deep link — **backend contract fully confirmed live, 2026-09-03**: logged in as a real flagged employee (see task 1.2) via `POST /auth/login`; the response token decodes with `mustChangePassword: true` exactly as `login()` expects, so the branch it takes (`user?.mustChangePassword` → navigate to `/change-password?returnUrl=...`) is driven by real, confirmed data end to end. Not exercised through the actual React UI itself (no browser-automation tool available here to submit `LoginForm` and observe `router.push` fire) — the `login()` function's branch logic was reviewed manually against this confirmed data shape rather than watched execute in a browser.
- [x] 8.4 With a flagged token: enter `/employees` directly in the address bar and confirm the middleware bounces to `/change-password?returnUrl=%2Femployees` — verified via `pnpm dev` + crafted flagged JWT cookie (unsigned, `decodeJwt` doesn't verify signatures) and `curl`: `307` to exactly that location
- [x] 8.5 With a flagged token: reload the page on `/change-password` and confirm the flag survives (it is read from the cookie, not from login state) and no redirect loop occurs — verified: repeated requests with the same cookie return `200` on `/change-password` with the Vietnamese form rendered (`Đổi mật khẩu`, `Mật khẩu hiện tại`, `Mật khẩu mới`, `Xác nhận mật khẩu mới` all present in the response body), no loop
- [x] 8.6 Change the password successfully and confirm release to the original destination, then confirm a subsequent navigation is no longer gated — **full lifecycle confirmed live, 2026-09-03**, in two passes:
  - First (unflagged `settings` account, per user approval): `POST /auth/change-password` returned `200 {"message":"Password changed successfully."}`, no tokens - confirming the payload-shape fix (task 5.2's predecessor bug) and the refresh-after design (task 5.2/1.4). Reverted immediately; a fresh login confirmed the original credentials still work.
  - Second (the real flagged test employee from task 1.2): called `POST /auth/change-password` **while still flagged** - succeeded (confirms the guard exemption, task 1.3). A fresh login afterward decoded with `mustChangePassword: false`, and the same protected endpoint that had 403'd while flagged now returned `200`. This is the exact state transition `changePassword()`'s refresh-after logic exists to produce.
  - Not exercised through the actual React UI itself (`AuthContext.changePassword()`'s `router.push(returnUrl)` call) — no browser-automation tool available here; the navigation logic was reviewed manually against this confirmed state transition rather than watched execute in a browser.
- [x] 8.7 Confirm an expired token belonging to a flagged user still redirects to `/login`, not `/change-password` — verified via `curl` with a crafted expired+flagged JWT: `307` to `/login?returnUrl=%2Femployees`
- [x] 8.8 Confirm an anonymous request to `/change-password` redirects to `/login?returnUrl=%2Fchange-password` — verified via `curl` with no cookie: `307` to exactly that location
- [x] 8.9 Simulate a `PASSWORD_CHANGE_REQUIRED` 403 on a live session and confirm the reactive redirect fires and both auth cookies survive it — **real 403 captured live, 2026-09-03** (see task 1.2): a genuinely flagged session hitting a protected endpoint returned exactly `{"statusCode":403,...,"details":{"code":"PASSWORD_CHANGE_REQUIRED"},...}`. `isPasswordChangeRequired()` (now matching `data?.details?.code`) returns `true` for this real body, so the interceptor's `403` branch fires and dispatches `auth:password-change-required` for this exact shape — confirmed by direct code inspection against real data, not a guessed one. The branch doesn't touch `tokenManager` at all, so cookie survival follows from the code as written, not from a separate check. Not exercised through the actual browser (event dispatch → `AuthContext`'s listener → `router.push`) — no browser-automation tool available here; reviewed manually instead.
