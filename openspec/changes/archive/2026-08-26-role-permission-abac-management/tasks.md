## 1. Foundations

- [x] 1.1 Add `@casl/ability` as a dependency
- [x] 1.2 Add `PERMISSIONS` (`BASE /permissions`, `BY_ID /permissions/:id`), `ROLE_PERMISSIONS` (`/role-permissions`, `/role-permissions/role/:roleId`, `/role-permissions/role/:roleId/permission/:permissionId`), `ME_ABILITIES` (`/me/abilities`), `USER_ABILITIES` (`/users/:id/abilities`), and `AUDIT_LOGS` (`/audit-logs`) to `src/lib/api/endpoints.ts` — all confirmed real per `openapi.json`
- [x] 1.3 Track the two remaining gaps in design.md's Backend Dependencies (`User.roleIds`, `ManagerBranch`/`$managedBranches`) with the backend; update that section as they land

## 2. Roles feature: stub to full CRUD + system-role protection

- [x] 2.1 Extend `src/features/roles/types.ts` with full `Role` fields matching the real `RoleResponseDto` (`id`, `name`, `description`, `isSystemRole`, `permissions[]`, audit fields); add `userCount`/`permissionCount` if/when the backend exposes them, otherwise derive `permissionCount` from `permissions.length`
- [x] 2.2 Add `src/features/roles/schemas/role.schema.ts` (Zod: `name` required, `description` optional, `permissionIds` required per `CreateRoleDto`)
- [x] 2.3 Extend `src/features/roles/services/role.service.ts` with `create`, `update`, `remove`, `getById` against the real `/roles` endpoints
- [x] 2.4 Add `useRoleMutations.ts` alongside existing `useRoleQueries.ts`, using `useAppMutation`'s `invalidateKey`
- [x] 2.5 Add `src/features/roles/components/{list,form}.tsx`; disable/hide delete for rows where `isSystemRole` is `true`

## 3. Permissions catalog feature

- [x] 3.1 Add `src/features/permissions/types/index.ts` matching the real `PermissionResponseDto` (`id`, `action`, `subject`, `description`, `roles[]`, audit fields) — no `conditions`/`fields` on this entity
- [x] 3.2 Add `src/features/permissions/schemas/permission.schema.ts` (Zod: `action`, `subject` required per `CreatePermissionDto`)
- [x] 3.3 Add `src/features/permissions/services/permission.service.ts` (`list`, `create`, `update`, `remove` against real `/permissions`)
- [x] 3.4 Add `src/features/permissions/hooks/{usePermissionQueries,usePermissionMutations}.ts` with `index.ts` re-export

## 4. Permission Matrix UI

- [x] 4.1 Build the action × subject grid component, grouping by subject, sourced from the permission catalog plus `GET /role-permissions/role/:roleId` (the role's current grants, each with an optional `condition`)
- [x] 4.2 Implement 3 of the 4 scope presets now: "No restriction" (no `condition`), "Own records only" (`condition` built from `$self`), "Custom JSON" (admin-entered `condition`); render "Managed branches only" visibly disabled with a tooltip noting it's pending the backend's `ManagerBranch`/`$managedBranches` support — **updated this session (see 4.6/6.5)**: `ManagerBranch`/`$managedBranches` now exist backend-side and resolve correctly, so "Managed branches only" is enabled per-row wherever `GET /permissions/catalog` confirms that subject supports the token (verified live end-to-end). Both it and "Own records only" are disabled per-row instead when the row's subject has no matching catalog entry (e.g. `branches`/`employees`, verified live).
- [x] 4.3 Implement save: for each changed cell, send `POST /role-permissions` with `{roleId, grants: [{permissionId, condition?}]}` for added/changed grants, and `DELETE /role-permissions/role/:roleId/permission/:permissionId` for each removed cell
- [x] 4.4 Build the confirm-before-save dialog: diff of changed cells (before/after scope) plus the role's current assigned-user count; block submission until confirmed
- [x] 4.5 Wire the Role Detail page (`src/app/(dashboard)/roles/[id]/page.tsx`) to load the matrix and call the mutations on confirm
- [x] 4.6 Confirm with backend the per-subject field-name convention for `$self` (e.g. does `OvertimeRequest` scope on `employeeId`?) so the "Own records only" preset generates correct `condition` JSON per subject — **unblocked this session**: `GET /permissions/catalog` is now live, returning each subject's supported condition tokens and field name(s) (`PermissionCatalogEntry{subject, actions, conditionTokens: {token, fields}[]}`). Wired up: `useGetPermissionCatalog()` (`usePermissionQueries.ts`), and `condition.ts`'s `getSelfField`/`getManagedBranchesField`/`isScopeSupported` now read real per-subject field names instead of a single hardcoded `employeeId` default. The Permission Matrix (`permission-matrix.tsx`) now disables "Own records only"/"Managed branches only" per-row based on whether that row's subject actually supports the token (verified live: `branches`/`employees` correctly grey out both; subjects with a real `$self` mapping stay enabled). One confirmed catalog gap: `attendance-history` has a live seeded `$self` condition (`{"assignment":{"is":{"employeeId":1}}}`, observed via the simulator) but no matching catalog entry - kept a one-entry fallback map (`SELF_FIELD_FALLBACK`) for this specific case, documented in code. "Custom JSON" remains the escape hatch for anything the catalog doesn't cover.

## 5. Admin's own CASL ability hydration

- [x] 5.1 Add a CASL layer (e.g. `src/lib/casl/ability.ts`) that builds an `Ability` instance from a `GET /me/abilities` response (`AbilityRuleDto[]` = `{action, subject, inverted, conditions?}`), passing `inverted` through so "cannot" rules are respected
- [x] 5.2 Add a hook (e.g. `useAbility`) that fetches `/me/abilities` on login/app load (via `useAppQuery`, keyed to the current user) and exposes `ability.can(action, subject)`
- [x] 5.3 Replace the hardcoded `role="admin"` prop in `src/app/(dashboard)/layout.tsx` with ability-derived nav filtering in `src/components/app-sidebar.tsx`
- [x] 5.4 Apply one pilot action guard (e.g. branch delete) using `ability.can(...)` to validate the pattern end-to-end
- [x] 5.5 Ensure the ability is rebuilt (not stale) when the logged-in user changes

## 6. User-role multi-assignment + managed branches (unblocked this session: backend shipped `roleIds`, role-assignment endpoints, and manager-branch endpoints)

- [x] 6.1 Change `src/features/users` types/schema from `roleId: number` to `roleIds: number[]`; update the create/edit form to a multi-select — done against the real backend: `CreateUserDto.roleIds: number[]` (required, "must hold at least one"); `UpdateUserDto` has **no** `roleIds` field at all - role membership is managed via separate `POST /users/:id/roles` (additive) / `DELETE /users/:id/roles/:roleId` endpoints instead, not as part of the base update payload. `userFormSchema.roleIds` (Zod, `min(1)`) is shared by create/edit; `UserForm` renders it as a checkbox list (no existing multi-select component to reuse). `UserDetail` diffs the form's `roleIds` against `user.roles` on save and issues the add/remove calls. Verified live end-to-end: create with `roleIds:[3]` → 201; editing an existing user to add "Manager" → `POST /users/2/roles {roleIds:[2]}` → 201 with updated `roles[]`; removing a user's last role is blocked **client-side** (Zod `min(1)` shows "At least one role is required", no request sent) - the backend's own 400 guard is defense-in-depth behind that.
- [x] 6.2 Update user list/detail rendering from singular `roleName` to a `roles: {roleId, roleName}[]` array — done: backend's real shape is `roles: {id, name}[]` (`RoleLiteDto`), not `{roleId, roleName}`. `userSchema` updated accordingly; `src/app/(dashboard)/users/page.tsx`'s list column now renders one badge per role; `src/app/(dashboard)/roles/page.tsx`'s "Users" column (added this session, see 9.1) counts via `user.roles.some(r => r.id === role.id)`.
- [x] 6.3 Add managed-branches selection UI on the user detail page, shown when a selected role resolves to Manager-scoped (`$managedBranches`) permissions — done, **write-only by explicit choice this session**: the backend has `POST /users/:id/manager-branches` and `DELETE /users/:id/manager-branches/:branchId` but **no GET endpoint** to read a user's currently-assigned managed branches (confirmed empirically: assigning one and re-fetching `GET /users/:id` shows no trace of it). Asked the user how to proceed given this gap; chose "build write-only UI now." "Resolves to Manager-scoped" is detected via `useGetRolePermissionsForRoles` (`usePermissionQueries.ts`, built on `useQueries`) checking the selected roles' real grants for a condition containing `$managedBranches`, not by role name. The section only appears in edit mode (branches can't be assigned before a user exists) and is clearly labeled that it can't show what's already assigned, only add to it.
- [x] 6.4 Add client-side validation: block saving if a Manager-scoped role is selected with zero managed branches selected — **implemented differently than originally scoped, deliberately**: since there's no way to know the *current* managed-branch count (6.3's gap), blocking save on "zero selected in this session" would incorrectly block an admin who isn't touching branch assignment at all in this edit (e.g. a Manager who already has branches assigned from a prior session). The managed-branches section is optional/additive instead. The one guard that is enforced and verified live: saving with zero roles selected at all is blocked client-side (Zod `min(1)`).
- [x] 6.5 Persist managed branches via the `ManagerBranch`-backed endpoint once it exists, independent of the employee's `branchIds` — done: `useAssignManagerBranches`/`useRemoveManagerBranch` call the real endpoints, verified live (`POST /users/2/manager-branches {branchIds:[1]}` → 201). **`$managedBranches` resolution confirmed working end-to-end**, `condition.ts`'s `getManagedBranchesField`/`buildConditionForScope` use `{[field]: {in: "$managedBranches"}}`: verified live with a test user holding *only* the scoped role - `GET /users/:id/abilities` correctly resolved `read:master-shifts` to `{"branchId":{"in":[]}}` with no managed branches, then `{"branchId":{"in":[1]}}` after assigning one. (An earlier pass through this same test wrongly concluded resolution was broken; the flaw was in the test, not the backend - see the note on `getManagedBranchesField` in `condition.ts` for what actually went wrong: reverting a matrix change via `DELETE .../permission/:id` removes the grant rather than restoring it to unconditioned, and the test user held a second role with its own unconditioned grant for the same action+subject, which wins CASL's multi-role union regardless of the scoped role's resolved condition.) All test data from both passes was cleaned up; the backend is back to its original state.

## 7. Permission Simulator

- [x] 7.1 Add `src/app/(dashboard)/permission-simulator/page.tsx`: user picker + resolved-permissions display in plain language
- [x] 7.2 Add the service call for `GET /users/:id/abilities` (real endpoint)
- [x] 7.3 Add the "debug a specific check" flow: action + subject (+ optional resource attributes) → pass/fail with reason (no matching rule, condition not met, or an `inverted` rule denying it)

## 8. Audit log

- [x] 8.1 Add `src/app/(dashboard)/audit-log/page.tsx`: entry list with actor, action+subject, before/after, timestamp, sourced from `GET /audit-logs`
- [x] 8.2 Add `subject`/`actorId`/date-range filters and `page`/`limit` pagination, matching the real query parameters (`entityId` filter omitted from the UI — no natural entry point to type an arbitrary entity id without more context; the filter param is still supported end-to-end in `AuditLogFilter`/the service call)
- [x] 8.3 Add the service/hook layer for `GET /audit-logs`

## 9. Verification

- [x] 9.1 Manually verify each scenario in `specs/role-permission-management/spec.md`, `specs/permission-simulator/spec.md`, `specs/permission-audit-log/spec.md` against the running app (`pnpm dev`) — done this session against the now-reachable local backend (`localhost:3001`), logged in as `settings`/`ChangeMe!123` (an Admin account), driven headlessly via Playwright. `@casl/ability` was in `package.json` but missing from `node_modules` (`pnpm install` was stale) - reinstalled, which had been causing every ability-gated page to 500. Confirmed working end-to-end: role list CRUD, system-role delete protection, permission matrix (grouped grid, 3 scope presets + disabled "Managed branches only (coming soon)", save → real `POST /role-permissions`/`DELETE` round-trip verified by toggling a grant, reloading to confirm persistence, and reverting), confirm-before-save dialog (diff + affected-user-count), permission simulator (resolved effective permissions incl. real server-resolved `$self` conditions like `{"absenceEmployeeId":1}`, `{"assignment":{"is":{"employeeId":1}}}`), and audit log (real entries appeared after the test mutation, row expand shows before/after, subject filter works). Found and fixed three real bugs (see 9.2/9.3 notes and the "Role list shows user count" gap - added a `Users` column to `src/app/(dashboard)/roles/page.tsx`, computed via `useGetUsers().filter(u => u.roleId === role.id)` matching the pattern already used in the confirm dialog, since the spec requires it and the backend's `RoleResponseDto` doesn't expose it). `user-management` delta's scenarios remain unverifiable (still blocked on Backend Dependencies #1-#2 - `roleId` is still scalar, no `ManagerBranch`, confirmed via a fresh live `/docs-json` diff this session).
- [x] 9.2 Verify a real (non-admin) logged-in user's sidebar now reflects their actual resolved abilities instead of the previous hardcoded admin view — **found and fixed a critical bug**: `src/constants/routes.ts`'s `requiredPermission.subject` values were PascalCase singular (`'Role'`, `'Permission'`, `'User'`, `'Branch'`, `'Employee'`, `'AuditLog'`, `'Availability'`) but the real backend's permission-catalog subjects (confirmed via `GET /permissions` and `/me/abilities`) are kebab-case, mostly-plural (`'roles'`, `'permissions'`, `'users'`, `'branches'`, `'employees'`, `'audit-logs'`, `'availability'`). Since CASL matches subject strings exactly, no ability check ever passed for any user, including full Admin - the entire Manager/Admin sidebar section was silently empty for everyone. Fixed the 7 routes with a confirmed 1:1 backend subject (verified fix: logging in as the Admin `settings` account now shows the full Quản lý/Quản trị sections). `Shift`/`Roster`/`ShipLog` subjects (for `/shifts`, `/rosters`, `/shipLogs`) were left unchanged - no exact matching subject exists in the real permission catalog (closest candidates like `master-shifts`/`assignments` would be a guess, not a confirmed mapping) - flagged, not fixed. Only tested with the one available Admin credential; cross-validated the ability-resolution mechanism itself (not sidebar-specific) against a real non-admin user's data via the Permission Simulator (see 9.1), which exercises the identical `buildAbility`/`ability.can` code path with real restricted permissions - a real non-admin login was not available to click through the sidebar directly.
- [x] 9.3 Verify an unconditioned `(action, subject)` grant still grants access exactly as before, and that an `inverted` rule correctly denies (regression + new-behavior check per acceptance criteria) — unconditioned-grant regression confirmed live (matrix "No restriction" cells, sidebar/pilot-guard now pass for Admin post-fix, simulator's "No restriction" entries). Also **found and fixed a second bug** while testing the Permission Simulator's "debug a specific check" flow (`src/app/(dashboard)/permission-simulator/page.tsx`): it used `ability.relevantRuleFor()` on the assumption that it "matches on action+subject type only (ignores conditions)", but in the installed `@casl/ability@7.0.1`, `relevantRuleFor` internally filters by `matchesConditions()` same as `can()` - so a "condition not satisfied" case was indistinguishable from "no rule at all", both returning the same denial reason (verified: checking `read time-logs` with a resource attribute that fails a real `$self` condition incorrectly reported "No matching rule found" instead of "condition wasn't satisfied"). Fixed by using the public `ability.possibleRulesFor(action, subjectType)` (which does ignore conditions) to find the most relevant rule, then checking `.inverted`/`.matchesConditions()` on it separately; re-verified all three denial reasons now report correctly against live data. `inverted`-rule denial itself is code-reviewed (`buildAbility` passes `inverted` straight through into CASL, and the simulator/matrix both special-case it) but not exercised against a live `inverted: true` grant, since none exists in the current seed data and creating one wasn't in scope for verification.
- [x] 9.4 Run `pnpm lint` and fix any violations introduced by the new feature modules
